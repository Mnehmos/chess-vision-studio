# RSI batch jobs — paused 2026-06-28 (resume tomorrow)

Both long-running CPU jobs were **stopped** to free the workstation (user needed the CPU).
Neither lost meaningful work. Re-launch when away from the box.

## Why they were stopped
16-logical-core box was ~2.5x oversubscribed (proc queue 41, CPU 100%): the 12-worker
d20 relabel fleet + the loglmr gauntlet were starving the **live Lichess bot**, causing
bullet time-outs and first-move aborts. Bot itself was healthy (still streaming Lichess),
just CPU-starved. Bot left running at Normal priority.

## 1) d20 relabel fleet — RESUMABLE, zero loss
Was ~64% done (labeledNN.jsonl ≈ 3.4 MB of 5.2 MB shards, 12 shards). Workers skip rows
their output already has, so resume continues exactly where each shard stopped.

Resume (from `F:\Github\chess-vision-studio`):
```
& "C:\Users\mnehm\AppData\Local\Programs\Python\Python312\python.exe" `
  arena/relabel-fleet.py --no-reshard --out-dir F:/tools/gen9-labeled-d20 `
  --workers 12 --depth 20 --hash 256
```
`--no-reshard` reuses the existing `shard00..11.jsonl` (do NOT drop it, or it re-derives/
reshards and the partial `labeledNN.jsonl` won't line up). Monitor:
`type F:\tools\gen9-labeled-d20\labeled00.jsonl.log`

## 2) loglmr gauntlet (search backlog item #4: log-based LMR table, --loglmr)
Same-binary A/B, base vs `--loglmr`. 273 completed games archived to
`F:\tools\gate-loglmr.partial-20260628.pgn` (cutechess can't resume mid-tournament, so
it was killed rather than suspended — suspending corrupts the wall-clock TCs).

Re-run fresh tomorrow (bump `-concurrency` to 12 when nothing else is heavy):
```
F:\tools\cutechess\cutechess-1.3.1-win64\cutechess-cli.exe -tournament gauntlet `
  -engine name=base   cmd=F:\Github\chess-vision-studio-rust-engine\target\release\uci.exe arg=--nnue arg=F:\Github\chess-vision-studio-rust-engine\target-cvs\matrix-raw.json `
  -engine name=loglmr cmd=F:\Github\chess-vision-studio-rust-engine\target\release\uci.exe arg=--nnue arg=F:\Github\chess-vision-studio-rust-engine\target-cvs\matrix-raw.json arg=--loglmr `
  -each proto=uci tc=10+0.1 -openings file=F:\tools\iid-gate-openings.epd format=epd order=random `
  -repeat -games 2 -rounds 150 -concurrency 12 -recover -ratinginterval 50 -pgnout F:\tools\gate-loglmr.pgn
```
To reuse today's 273 games for scoring, `cat` the archived partial + the new pgn before
`gauntlet:score` (same engine defs, so they combine cleanly).

## Engine commits landed today (rust-engine, branch develop — all gated/off by default)
- `75be171 10:52` port smarttime soft/hard split to analyze.exe go path (the bot binary)
- `244ad59 19:46` BUG1: ply-adjust mate scores on TT store/probe (`--matett`, gated)
- `d0fd3a7 21:05` log-based LMR reduction table (`--loglmr`, gated) ← the job above gates this
